# No front matter at all

This file opens straight into markdown with no `---` block above it. Tova has
to cope: title from the filename, no tags, no dates but the file's own.

A note written by hand, or dropped in from somewhere else, looks like this.
